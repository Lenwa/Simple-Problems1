def alphabetical():
    prefixes = 'JKLMNOPQ'
    suffix = 'ack'
    for letter in prefixes:
        if letter == 'O' or 'Q':
		    print letter + 'u' + suffix
        else:
            print letter + suffix
        
alphabetical()
